rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Explicitly allow listing files at the root level and recursively
    allow list: if request.auth != null;

    match /{allPaths=**} {
      // Allow individual file access and modifications
      allow read, write: if request.auth != null;
    }
  }
}
